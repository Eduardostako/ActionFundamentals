name: Docker Test
description: some description
on:
  workflow_call:
    inputs:
      who-to-greet:
        description: 'The person to greet'
        type: string
        required: true
        default: World
        
runs:
  using: docker
  image: Dockerfile
  args:
  - ${{ inputs.who-to-greet }}
  
